<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Editar Caso - {{ case.case_number }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: {{ settings.primary_color|default:"#0057B7" }};
        }
        .btn-primary {
            background: var(--primary-color) !important;
            border-color: var(--primary-color);
        }
        .header {
            background: var(--primary-color);
            color: white;
            padding: 1rem;
            border-radius: 8px;
            margin-bottom: 1.5rem;
        }
    </style>
</head>
<body class="bg-light">
    <div class="container py-5">
        <!-- Encabezado -->
        <div class="header text-center mb-4">
            <h2><i class="fas fa-edit"></i> Editar Caso: {{ case.case_number }}</h2>
            <p>Modifica los datos del caso</p>
        </div>

        <!-- Mensajes -->
        {% if messages %}
            {% for message in messages %}
                <div class="alert alert-{{ message.tags }} alert-dismissible fade show" role="alert">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}
        {% endif %}

        <!-- Formulario -->
        <form method="post">
            {% csrf_token %}

            <!-- Errores del formulario -->
{% if form.errors %}
    <div class="alert alert-danger">
        <strong>Hay errores en el formulario:</strong>
        <ul>
            {% for field, errors in form.errors.items %}
                {% for error in errors %}
                    <li>{{ field }}: {{ error }}</li>
                {% endfor %}
            {% endfor %}
        </ul>
    </div>
{% endif %}

            <div class="row">
                <div class="col-md-6">
                    <h5>Solicitante</h5>
                    {{ form.applicant_name }}
                    {{ form.applicant_id }}
                    {{ form.applicant_phone }}
                    {{ form.applicant_email }}
                </div>
                <div class="col-md-6">
                    <h5>Involucrado</h5>
                    {{ form.involved_name }}
                    {{ form.involved_id }}
                </div>
            </div>

            <h5>Detalles del Conflicto</h5>
            {{ form.conflict_description }}
            {{ form.location }}
            {{ form.conflict_type }}
            {% if form.other_conflict_type %}
                <div class="mb-3">{{ form.other_conflict_type }}</div>
            {% endif %}

            <h5>Resolución</h5>
            {{ form.resolution_method }}
            {% if form.other_resolution_method %}
                <div class="mb-3">{{ form.other_resolution_method }}</div>
            {% endif %}

            <h5>Observaciones</h5>
            {{ form.notes }}

      
</div>

<!-- Errores generales del formulario -->
{% if form.non_field_errors %}
    <div class="alert alert-danger">
        {{ form.non_field_errors }}
    </div>
{% endif %}

<!-- Botón de guardar -->
<div class="mt-4">
    <button type="submit" class="btn btn-primary">
        <i class="fas fa-save"></i> Guardar Cambios
    </button>
    <a href="{% url 'core:admin_case_detail' case.id %}" class="btn btn-outline-secondary">
        <i class="fas fa-times"></i> Cancelar
    </a>
</div>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>